# Session: 2026-06-01

## What I did
- Built Ex5 end-to-end: pgvector tables → Titan v2 embeddings → cosine search → frontend UI
- Post-submission debugging: fixed circular import + pgvector string format bug
- Ran live backfill (Bedrock) + export: 38 real Titan v2 vectors in TSVs
- Validated end-to-end: job_006 → cv_004 (0.54), confirmed threshold=0.5 is appropriate
- Clarified Ex5 vs Ex6 scope: Ex5 is retrieval improvement; LLM comprehension is already in the model

## Key Learnings
- `str(numpy_array)` silently produces spaces/newlines — always use explicit join for pgvector literals
- Circular import pattern: module-level import of A from B, while B imports A's module → lazy import inside function body is the clean fix
- Semantic similarity gap (0.54 vs 0.38) validates threshold — not all "close" candidates are actually close
- Ex5 semantic search ≠ LLM comprehension: embeddings fix the retrieval layer; the agent's language understanding is pre-trained

## For Next Session
- Plan Ex6: HR agent (Strands + Gmail MCP + human-in-loop)
- Real TSVs (38 rows) should be reloaded in TF Projector for real semantic clusters

## Blockers
- None

## Commits This Session
- `e54f1bc` docs(ex5): update submission git log with post-submit fixes
- `324a07b` fix(search): format pgvector query string as [f1,f2,...] not str(numpy)
- `78e6783` fix(ingest): lazy-import embeddings.service to break circular import
- `1a5d3ef` feat(ex5): self-contained mock TSV export + projector files
- `46e97e8` fix(ex5): use .venv/bin/python in backfill + export scripts
- `90b4a8a` SUBMIT: Ex5 — semantic search with embeddings complete
- `c54ebdd` docs(ex5): NotebookLM source bundle — overview + retrieval-eval + journal
- `a1f8c98` docs(ex5): draw.io diagrams — embedding pipeline + data model
- `2d6a5d3` feat(ex5-09): TSV export + retrieval evaluation doc
- `6058a73` feat(ex5-08): frontend: suggested candidates + recommended positions
- `66dbf0e` feat(ex5-07): match endpoints + ingest auto-embed hook — 7/7 tests
- `76dd72f` feat(ex5-06): grounded match explainer — 5/5 tests
- `38d09b5` feat(ex5-05): vector search + exclusion + threshold — 8/8 tests
- `85b2113` feat(ex5-04): embedding service + backfill (sha guard, idempotent) — 5/5 tests
- `fa4ab26` feat(ex5-03): Titan embed on BedrockClient + MockEmbeddingClient — 6/6 tests
- `b8720b7` feat(ex5-02): deterministic embedding text builders — 6/6 tests
- `dd82d70` feat(ex5-01): pgvector embedding tables + SQLite seam — 3/3 tests
- `c3cd180` eX5 begins (planned)
- `58222aa` eX5 begins (planned)
- `1117767` docs(ex5): master plan + segmented build plan (00–09, +10 optional)
