# Session: 2026-05-31

## What I did
- Implemented Ex4 segments 05–08: executor (read-only seam), answerer (grounded multi-turn), orchestrator + POST /api/chat endpoint, Chat UI with RetrievalPanel
- Module rename: app/pipeline→ingest, app/query→chat (148/148 green)
- Fixed Postgres executor bug: AsyncSession.get_bind() returns sync Engine; fixed by importing _async_engine from app.db
- Live demo via Playwright against real Postgres + Bedrock; caught executor bug live
- Created submission artifacts: draw.io pipeline diagram (PNG+SVG), git-log.txt, walkthrough.txt

## Key Learnings
- AsyncSession.get_bind() returns sync Engine — only caught at demo time; SQLite path was fine, Postgres path was dead code until container ran → [[feedback_async-engine-postgres]]
- Script outputs to /tmp are invisible; user called it out — all outputs belong in repo → [[feedback_script-outputs-repo-path]]
- draw.io + PNG/SVG preferred over HTML+SVG for submission diagrams → [[feedback_diagram-preferences]] (updated)
- Monkeypatch paths updated: app.ingest.BedrockClient, app.chat.generator/answerer.BedrockClient → [[project_ex3-monkeypatch-import-binding]] (updated)
- Node 18 too old for Vite 8 → [[reference_node-version]]

## For Next Session
- Ex5: Embeddings / vector search
- "Compatibility" queries (most compatible candidate) deferred to Ex5 — SQL can't do semantic similarity
- Consider sql-v2.txt schema annotation (3-line note about candidate_skills ↔ position_requirements having no FK) — small but prevents wrong joins

## Commits This Session
- d42459b feat(ex4-01) through 212c4f2 docs(ex4) — 22 commits, Ex4 complete
